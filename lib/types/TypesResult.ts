export type ResultUpdate = {
  wasUpdated: boolean;
};

export type ResultDelete = {
  wasRemoved: boolean;
};

export type ResultDeletes = {
  removed: number;
};

export type ResultSave = {
  id: number;
};
